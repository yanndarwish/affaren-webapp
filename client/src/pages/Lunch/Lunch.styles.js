import styled from "styled-components";
import { constant } from "../../assets/common/common.styles";
export const MuteButtonWrapper = styled.div`
    position:absolute;
    bottom:${constant.MARGIN_M}px;
    left:${constant.MARGIN_M}px;
    z-index: 10;

`