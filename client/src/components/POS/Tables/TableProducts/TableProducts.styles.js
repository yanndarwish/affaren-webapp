import { constant } from "../../../../assets/common/common.styles"
import styled from "styled-components"

export const FormulaButtonWrapper = styled.div`
	position: absolute;
	bottom: ${constant.MARGIN_M}px;
	left: 50%;
	transform: translateX(-50%);
`
