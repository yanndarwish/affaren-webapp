import { Paper, Box } from "@mui/material"
import { StyledProductCard } from "./ProductCard.styles"

const ProductCard = ({ id, name, image, price, taxe, theme }) => {
    
	return (
		<StyledProductCard theme={theme} className="product-card">

		</StyledProductCard>
	)
}

export default ProductCard
